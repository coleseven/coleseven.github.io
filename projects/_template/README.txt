Copy this folder, rename it to your project slug,
and replace the placeholder text in index.html.

Don't forget:
1. Fill in the tech tags at the top (see the comment in index.html
   for which color class maps to which category).
2. Add the project to /projects/index.html as a new card.
3. If it uses tools not already shown, add them to the Skills
   section on the homepage (/index.html).
